{
  "name": "recubed-scripts",
  "version": "1.0.0",
  "description": "package.json for recubed cleanup",
  "main": "index.js",
  "scripts": {
    "build": "prettier --write ./src/**/*.ts && tsc ./src/**/*.ts --outDir dist"
  },
  "author": "colack",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  }
}
